{
  "name": "voi-staking",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "clean": "turbo clean && rimraf node_modules .turbo dist yarn.lock",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "prettier": "^3.1.0",
    "rimraf": "^5.0.5",
    "turbo": "latest"
  },
  "packageManager": "yarn@1.22.19",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "@agoralabs-sh/avm-web-provider": "1.7.0",
    "@txnlab/use-wallet-react": "3.5.0"
  }
}
